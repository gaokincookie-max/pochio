# v0.1 実装メモ

## 挙動判定順
1. 特殊組み合わせ
2. 装飾
3. 表情
4. 色
5. 大きさ

各接触で上位から判定し、最初に成立した `弾ける` または `くっつく` を採用します。同じレイヤー内で両方成立する場合は弾ける側を優先しています。

## 仮閾値
- 低速: 3.5 以下
- かなり低速: 1.8 以下
- 高速: 8.0 以上
- 非常に高速: 12.0 以上
- 長時間生存: 18秒以上
- 射出直後: 約2.8秒以内
- 壁接触直後: 約1.2秒以内
- 周囲判定半径: 約110px

これらはプレイ感を見て変更する前提です。

## 接着
柔らかいConstraintを使用。通常はつながったままですが、大きく引き伸ばされた場合または非常に大きい相対速度が掛かった場合のみ切れます。再会・復縁系履歴を成立可能にするための仕様です。

## 表情変化
基本固定。長時間生存かつ接触履歴がかなり蓄積した個体などに、ごく稀に変化判定が入ります。最大2回です。今後、専用条件へ置き換え可能です。

## 未実装として残している挙動条件
サイズ変化を要求する条件は、サイズ変化システム自体を採用していないため無効です。データベースには残しています。

## 役
★1〜★5の70役をデータ化し、すべて得点判定の候補にしています。非常に複雑な履歴・因果関係役の一部は、v0.1では保持している履歴から近似判定しています。★6はまだ未作成です。

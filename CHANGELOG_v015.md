# ぽちょ v0.1.5

## 色レイヤーPOP一時停止
- 色レイヤーの「弾ける」条件35件をすべて `disabled` に変更。
- 条件定義そのものは削除せず `data.js` とExcelデータベースに保持。
- 大きさPOP 7件の停止も継続。
- 色の「くっつく」条件33件は変更なし。
- 表情・装飾・特殊組み合わせのPOP条件は変更なし。
- 射出直後0.4秒のPOP保護は継続。

## 目的
大きさPOP停止後も弾け頻度が高かったため、色POPが頻度にどの程度寄与しているか切り分ける。

# ぽちょ v0.1.4 変更点

## 大きさレイヤーのPOP条件を一時全無効化
対象: size-5, size-6, size-10, size-11, size-15, size-16, size-17（計7件）

- 条件自体は削除していません。
- `active: false`, `status: disabled` とし、データベースにも同じ状態を記録します。
- 大きさレイヤーのSTICK条件は従来通り有効です。
- 色・表情・装飾・特殊組み合わせのPOP条件はv0.1.3のままです。
- 射出直後0.4秒のPOP保護も維持します。

### 狙い
現在の「すぐ弾ける」挙動の主因を切り分けます。大きさは最下層かつ全ぽちょが必ず持つ属性なので、POP条件を一度ゼロにして他レイヤーだけの弾け頻度を観察します。
